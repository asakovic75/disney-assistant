# 🤖 Умный ассистент "Пиксель"

Этот репозиторий содержит исходный код для веб-приложения "Пиксель" — чат-бота, способного отвечать на вопросы о произведениях The Walt Disney Company на основе локальной базы данных.

Этот проект является частью большой [базы знаний о вселенной Disney](https://github.com/asakovic75/disney-knowledge-base).

## ✨ Ключевые возможности

- **Запросы на естественном языке:** Задавайте вопросы так, как если бы вы общались с человеком.
- **Точные ответы на основе данных:** Ассистент использует только информацию из предоставленной базы знаний, что исключает выдумки и неточности.
- **Поддержка сложных запросов:** Способен обрабатывать запросы с несколькими условиями, фильтрами и сравнениями (например, "найди мультфильм с рейтингом выше 8 и бюджетом меньше 50 млн").
- **Простой и чистый интерфейс:** Минималистичный дизайн, созданный для удобного взаимодействия.

## 🛠️ Технологический стек

- **Python:** Основной язык программирования.
- **Streamlit:** Для создания интерактивного веб-интерфейса.
- **Pandas:** Для загрузки и обработки данных из CSV-файла.
- **Groq API & OpenAI Library:** Для доступа к языковой модели LLaMA, которая выполняет анализ запроса и поиск ответа в данных.

## ⚙️ Как это работает

1.  Приложение загружает данные из файла `ПроизведенияП.csv`.
2.  Все данные преобразуются в единый текстовый блок, который служит "контекстом" или базой знаний для модели.
3.  Вопрос пользователя вместе с этим контекстом и строгими инструкциями (промптом) отправляется к языковой модели.
4.  Модель анализирует запрос, находит релевантную информацию в предоставленных данных и генерирует ответ в заданном формате ("Рассуждения" и "Ответ").

## 🚀 Локальный запуск

Чтобы запустить приложение на вашем компьютере, выполните следующие шаги:

1.  **Клонируйте репозиторий:**
    ```bash
    git clone https://github.com/asakovic75/disney-assistant.git
    cd disney-assistant
    ```

2.  **Установите зависимости:**
    ```bash
    pip install -r requirements.txt
    ```

3.  **Настройте API-ключ:**
    Создайте файл `.streamlit/secrets.toml` и добавьте в него ваш ключ от Groq API:
    ```toml
    GROQ_API_KEY="ВАШ_API_КЛЮЧ_ЗДЕСЬ"
    ```

4.  **Запустите приложение:**
    ```bash
    streamlit run app.py
    ```
